用户指定：只要进行联网搜索，优先使用 searxng skill。

## 长期偏好与配置

### 模型使用策略
- **默认对话模型**：Gemini 3 Flash
- **Fallback 链**：Kimi 2.5 → Gemini 3 Pro → Qwen 3 Max
- **子代理默认**：Kimi 2.5（代码/复杂任务优先）
- **Token 策略**：使用免费模型时不刻意限制，保证回复完整性

### 平台操作规则
- **Telegram 媒体发送**：直接使用原生 `message` 工具，禁止使用 QQBot 的 `<qqimg>` 标签
- **语音回复**：使用 gTTS 生成中文语音，支持语音消息格式
- **双账号推送**：重要通知需同时发送到 8304964064 和 8505689440

### 自动化任务
- **健康助手周报**：每周日 18:00 自动发送健身总结
- **健康助手月报**：每月 1 日 10:00 自动发送月度分析
- **Gateway 维护重启**：每周一凌晨 4:00 自动重启（先保存记忆，确保无运行中任务）

### 记忆管理规则
- 每日记忆写入 `memory/YYYY-MM-DD.md`
- 长期重要信息（决策、偏好、配置）需同步更新到 `MEMORY.md`
- 健身数据持久化存储在 `fitness.log`

### 代理协作规则
- **健身记录**：用户要求记录健身情况时，调用 `health-assistant` 代理处理
  - 每次记录必须包含：记录时间、地点（杭州）、天气情况
  - 数据格式参考：`[日期] 健身记录` 开头，包含记录时间、地点、天气、训练时间和内容
